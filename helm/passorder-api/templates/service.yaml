apiVersion: v1
kind: Service
metadata:
  name: passorder-api
  namespace: default
  labels:
    app: passorder-api
spec:
  type: ClusterIP
  selector:
    app: passorder-api
  ports:
    - protocol: TCP
      port: 80
      targetPort: 3000